services:
  centaurion:
    build:
      context: ../../..
      dockerfile: Cognitive-Company/CI-CD-automations/Docker-containerization/Dockerfile
    container_name: centaurion
    environment:
      - API_KEYS=${API_KEYS:-}
      - PORT=8000
    ports:
      - "8000:8000"
    restart: unless-stopped
