---
name: __TOOL_NAME__
description: __ONE_LINE_DESCRIPTION__
origin: tool_forge
job_id: __JOB_ID__
stage: staging
---

# __TOOL_NAME__

__ONE_LINE_DESCRIPTION__

## Lifecycle

1. **Scaffold** — generated by Cortex inside an isolated worktree.
2. **Sandbox exec** — `test.py` runs inside an isolated container.
3. **Promotion** — surfaced to operator via Routing Gate; merged on approval.

## Routing-Gate classification

| Event | Route | Notes |
|---|---|---|
| `scaffold` | `ai_autonomous` | Worktree-isolated, no execution yet |
| `sandbox_exec` | `ai_autonomous` | Runs inside Sandbox; no network; container destroyed on completion |
| `promote` | `surface_to_human` | Always surfaces — writing executable code to canonical skills/ |
| `approve` / `reject` | `ai_autonomous` | Operator decision; logged side-effect |

## Files

- `SKILL.md` — this file (frontmatter + lifecycle notes)
- `impl.py` — implementation
- `test.py` — at least one assertion required by the forge

## Promotion criteria

This skill graduates from `_staging/` to `_promoted/` only if:

- Sandbox execution exits 0 with no timeout
- Operator approves the surfaced diff via Routing Gate
- (Phase 2) ≥5 successful invocations recorded for fast-track

Rejected scaffolds archive to `_failed/<job_id>-<tool_name>/` with a `REASON.txt`.
