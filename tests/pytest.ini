[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    docker: requires a live Docker daemon (skipped if unavailable)
    integration: end-to-end across forge + sandbox + routing gate
addopts = -ra --strict-markers
