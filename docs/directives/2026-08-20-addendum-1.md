# ADDENDUM-1 -- Completes 2026-08-20 Directives

> For: Centauri. Cold-start-safe. Completes truncated S3 (WO-024 rev2), delivers WO-025 in full, corrects dsh install, adds post-filing verification.

## A. CORRECTION -- dsh install (supersedes "npx @deepseek-ai/dsh")

Confirmed: not published to npm. Install from source at a pinned commit -- SHA is the version pin (stronger than semver for a developer preview):

```bash
git clone https://github.com/deepseek-ai/deepseek-harness
cd deepseek-harness && git checkout <PIN_SHA>   # record SHA in the Issue
# build + run web UI per repo README (record exact commands used)
```

Fence unchanged: runs OUTSIDE Omnigent policy -- no repo write, no factory access, no secrets beyond scoped Mautic-sandbox env, GTM data only.

## B. WO-024 rev2 -- S3 TASKS (complete)

1. Install pinned dsh from source (Section A). Record SHA + plugin list hash.
2. Use creator mode to build the plugin wrapper (do not hand-port): wrap the existing Mautic-nurture skill + aob-brand-voice content generation. Canonical skill stays runtime-agnostic in `~/.claude/skills/`; the dsh plugin is a thin wrapper. If spike fails, nothing is lost.
3. Model A/B/C in the same harness (provider-agnostic via DeepSeek/OpenRouter keys): DeepSeek default vs Qwen 3.8 vs one Claude model. Same plugin, same 10-task GTM batch.
4. Measure per model:
   - (a) token cost per accepted output
   - (b) quality pass-rate vs aob-brand-voice acceptance checks
   - (c) state pollution: force 5 mid-task failures; verify disposer rollback leaves zero residue (files, config, processes) and retry starts pristine
   - (d) load -> run -> unload -> reload cycle: residue = zero
   - (e) integration friction hours
5. Evaluate dynamic tool hiding (`ctx.inject`): expose only phase-relevant tools; record context-size delta.
6. Note only (do not build): dsh orchestrator mode (Claude Code/Codex as sub-agent plugins) = candidate shell for the factory's cross-vendor review cell. Trigger: factory P2 calibration passed. One paragraph.

Deliverable: one GitHub Issue update on #134 -- pinned SHA, measurement table (3 models x 5 metrics), harness-doctrine score (all 4 properties), ADOPT/DEFER + one paragraph. Malik gates. No progression past spike without approval.

## C. WO-025 -- FACTORY TRAJECTORY PROVENANCE (full text)

Gap: dsh attributes every agent-loop action to its source plugin; factory audit (Issues + archive) is coarser. Doctrine property 2 (AUDITABLE) requires per-action provenance.

Task: structured trajectory logging on the line -- per cell session, append-only JSONL:

```json
{"ts": "...", "wo": "WO-NNN", "phase": "build|review|verify", "action": "tool_call|file_write|...", "source": "skill/policy/prompt that caused it", "tool": "tool_name", "result": "success|fail|...", "cost": 0.0042}
```

Storage: `logs/trajectory/` or derived store (Tier-3 evidence -- NOT canonical wikis).

Surface: `/doctor` console route reads it; weekly compaction distills notable RCA entries to canonical (feeds property 3).

Acceptance: replay any merged WO and answer "which skill/policy caused action N?" for 100% of actions.

Priority: after factory P1, before first company-WO dispatch. Pairs with WO-026 (claim ledger): WO-025 = what agents DID; WO-026 = whether what they CLAIMED was true.
